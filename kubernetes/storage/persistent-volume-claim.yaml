apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: sqlite-pvc
  namespace: job-tracker

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: manual

  resources:
    requests:
      storage: 1Gi